on: push
name: publish to BGA
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: sftp://1.studio.boardgamearena.com
        port:22
        server-dir: stonespinearchitects/
        username: paxcow
        password: test
